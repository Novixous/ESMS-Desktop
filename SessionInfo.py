class SessionInfo:  
    def __init__(self, faceFrames, sessionBeggin, sessionEnd):  
        self.faceFrames = faceFrames  
        self.sessionBeggin = sessionBeggin
        self.sessionEnd = sessionEnd
        self.sections = []
