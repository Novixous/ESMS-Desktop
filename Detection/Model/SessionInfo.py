class SessionInfo:  
    def __init__(self, frames, sessionBeggin, sessionEnd, periods):  
        self.frames = frames  
        self.sessionBeggin = sessionBeggin
        self.sessionEnd = sessionEnd
        self.periods = periods
